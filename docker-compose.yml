services:
  api:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      RAILS_ENV: development
    volumes:
      - .:/rails
      - bundle:/usr/local/bundle
    command: bin/rails server -b 0.0.0.0 -p 3000

volumes:
  bundle:
